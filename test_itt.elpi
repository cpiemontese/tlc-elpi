%accumulate itt/lang.
accumulate itp.
accumulate itt/tactics.
accumulate itt/pp.

%main :- interactive andElimR [intro f, andER f] S.

identity (prod a (x\ a)).

% ---------------------- %
% ---- K combinator ---- %
% ---------------------- %

% [intro, intro, app x]
comb_K (prod a (x\ prod b (y\ a))).

% ---------------------- %
% ---- S combinator ---- %
% ---------------------- %

% [intro f, intro g, intro h, app f, app g, app h, app h]
comb_S (prod (prod a (x\ prod b (y\ c))) (_\ prod (prod a (x\ b)) (f\ prod a (x\ c)))).

% axioms of conjunction
% [intro, intro, andI 2 1]
andIntro (prod a (x\ prod b (y\ sum a (z\ b)))).

% [intro p, andEL p]
andElimL (prod (sum a (x\ b)) (y\ a)).

% [intro p, andER p]
andElimR (prod (sum a (x\ b)) (y\ b)).

% [intro, intro, andE, var 1, app, app 4 1, var 2]
implAnd (prod (prod a (x\ prod b (y\ c))) (_\ prod (sum a (x\ b)) (z\ c))).
